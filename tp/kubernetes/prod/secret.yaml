# Template only. Real values are generated at deploy time by `scripts/gen-k8s-secrets.sh`
# Never commit real credentials. In production, prefer SealedSecrets, External Secrets,
# SOPS-encrypted manifests, or an external KMS (OCI Vault, HashiCorp Vault).
apiVersion: v1
kind: Secret
metadata:
  name: gp-prod-db
  namespace: gp-prod
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "CHANGE_ME_BEFORE_APPLY"
  MYSQL_USER: "appuser"
  MYSQL_PASSWORD: "CHANGE_ME_BEFORE_APPLY"
  MYSQL_DATABASE: "gestion_produits"
